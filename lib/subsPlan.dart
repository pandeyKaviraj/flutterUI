import 'package:flutter/material.dart';

class subsPlan extends StatefulWidget {
  const subsPlan({Key? key}) : super(key: key);
  @override
  _subsPlanState createState() => _subsPlanState();
}

class _subsPlanState extends State<subsPlan> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [],
      ),
    );
  }
}
